/*import React from 'react';
import Image from 'next/image'

function Card(props) {
    return (
        <div style={styles.card}>
            <Image style={styles.image} src={props.card_number} width={800} height={500}/>
            {props.card_number}
        </div>
    );
}

const styles = {
    card : {
        width: '800px',
        height: '500px',
        backgroundColor: 'blue',
        border: '2px solid black',
        borderBox: 'border-box',
        fontSize: '2.5em',
        color: 'white'
    },
    image: {
        width: '100%',
        height: '100%'        
    }

    
}

export default React.memo(Card);*/